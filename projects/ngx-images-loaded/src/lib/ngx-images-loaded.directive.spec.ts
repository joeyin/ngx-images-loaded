import { NgxImagesLoadedDirective } from './ngx-images-loaded.directive';

describe('NgxImagesLoadedDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxImagesLoadedDirective();
    expect(directive).toBeTruthy();
  });
});
